# tenSVD Algorithm

Implementation of the **tenSVD** algorithm for efficient tensor compression and reshaping heuristics.

## Requirements
To run this code, you need the following R packages:
```r
install.packages("tensr")
install.packages("numbers")
